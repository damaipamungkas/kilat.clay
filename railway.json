{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "touch database/database.sqlite && php artisan migrate --force && php -S 0.0.0.0:$PORT -t public"
  }
}
